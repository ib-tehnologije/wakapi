meta {
  name: Sign up user
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/signup
  body: formUrlEncoded
  auth: none
}

script:pre-request {
  // Do not follow 3xx so we can assert the redirect status/location
  req.setMaxRedirects(0)
}

body:form-urlencoded {
  location: {{TZ}}
  username: testuser
  email: testuser@wakapi.dev
  password: testpassword
  password_repeat: testpassword
}

assert {
  res.status: eq 302
  res.headers['location']: eq /
}
