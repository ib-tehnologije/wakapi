meta {
  name: Projects
  type: http
  seq: 4
}

get {
  url: {{BASE_URL}}/projects
  body: none
  auth: bearer
}

auth:bearer {
  token: {{WRITEUSER_TOKEN}}
}

assert {
  res.body: contains </html>
}
