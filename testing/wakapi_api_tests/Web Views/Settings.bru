meta {
  name: Settings
  type: http
  seq: 5
}

get {
  url: {{BASE_URL}}/settings
  body: none
  auth: bearer
}

auth:bearer {
  token: {{WRITEUSER_TOKEN}}
}

assert {
  res.body: contains </html>
}
