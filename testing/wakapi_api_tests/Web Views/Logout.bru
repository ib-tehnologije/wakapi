meta {
  name: Logout
  type: http
  seq: 6
}

post {
  url: {{BASE_URL}}/logout
  body: none
  auth: none
}

script:pre-request {
  req.setMaxRedirects(0)
}

assert {
  res.status: eq 302
  res.headers['location']: eq /
}
